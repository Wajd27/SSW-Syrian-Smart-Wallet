# Legacy PostgreSQL schema

The current app uses **Firestore**, not PostgreSQL. The file `schema.sql` is kept only as a **historical reference** for the original relational model. Do not run it against production Firebase.
